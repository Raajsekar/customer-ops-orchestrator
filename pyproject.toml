
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "customer-ops-orchestrator"
version = "0.1.0"
description = "Multi-agent customer operations orchestrator with LangGraph and FastAPI"
requires-python = ">=3.10"
dependencies = []
